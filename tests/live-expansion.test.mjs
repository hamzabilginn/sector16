import test from 'node:test';import assert from 'node:assert/strict';import {modeLoadout,modeLimit,enemies,smokeBlocks,mapWinner} from '../shared/live-expansion.mjs';test('2.0 live rules',()=>{assert.deepEqual(modeLoadout('sniper'),['awp','pistol']);assert.equal(modeLoadout('arms',20)[0],'awp');assert.equal(modeLimit('ffa'),25);assert.equal(enemies({id:'a',team:'x'},{id:'b',team:'x'},'ffa'),true);assert.equal(smokeBlocks({x:0,y:1,z:-5},{x:0,y:1,z:5},[{x:0,y:0,z:0,until:20}],10),true);assert.equal(mapWinner(new Map([['a','refinery']]),'docks',['docks','refinery']),'refinery')});
